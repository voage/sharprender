services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    environment:
      - CHROME_PATH=/usr/bin/chromium-browser
      - PORT=8080
      - OPENAI_KEY=${OPENAI_KEY}
